import { NgAnimateCssGridDirective } from './ng-animate-css-grid.directive';

describe('NgAnimateCssGridDirective', () => {
  it('should create an instance', () => {
    const directive = new NgAnimateCssGridDirective();
    expect(directive).toBeTruthy();
  });
});
